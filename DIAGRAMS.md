# 📊 VoiceSwap - Diagramas Visuales

---

## 🎯 Diagrama de Usuario Simplificado

```
┌──────────────────────────────────────────────────────────────┐
│  USUARIO CON META RAY-BAN                                    │
└──────────────────────────────────────────────────────────────┘
                            │
                    Usuario dice:
              "Cambia 10 dólares de ETH a USDC"
                            │
                            ↓
┌──────────────────────────────────────────────────────────────┐
│  📱 MOBILE APP (iOS)                                         │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  1. Meta Wearables SDK captura audio                        │
│  2. Apple Voice Recognition → texto                          │
│  3. Envía a backend: POST /voice-command                     │
└──────────────────────────────────────────────────────────────┘
                            │
                            ↓
┌──────────────────────────────────────────────────────────────┐
│  ☁️  BACKEND (Node.js + Express)                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  1. Detecta idioma: Español                                  │
│  2. Traduce a inglés (OpenAI): "Swap $10 ETH to USDC"       │
│  3. Envía a Thirdweb AI Chat                                 │
└──────────────────────────────────────────────────────────────┘
                            │
                            ↓
┌──────────────────────────────────────────────────────────────┐
│  🤖 THIRDWEB AI CHAT API                                     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  Input: "Swap $10 ETH to USDC"                               │
│  Output: Transaction preparada                               │
│  {                                                            │
│    "type": "sign_swap",                                      │
│    "data": "0x3593564c...",                                  │
│    "description": "Swap 0.0032 ETH for ~10 USDC"            │
│  }                                                            │
└──────────────────────────────────────────────────────────────┘
                            │
                            ↓
┌──────────────────────────────────────────────────────────────┐
│  📱 CONFIRMACIÓN EN APP                                      │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  Swap 0.0032 ETH → 10.15 USDC                               │
│  Gas: FREE ✅                                                │
│                                                               │
│  [Confirmar]  [Cancelar]                                     │
└──────────────────────────────────────────────────────────────┘
                            │
                  Usuario confirma
                            ↓
┌──────────────────────────────────────────────────────────────┐
│  ⚙️  THIRDWEB ENGINE (Account Abstraction)                   │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  1. Crea UserOperation (ERC-4337)                            │
│  2. Gas sponsorship automático                               │
│  3. Envía a Unichain                                         │
└──────────────────────────────────────────────────────────────┘
                            │
                            ↓
┌──────────────────────────────────────────────────────────────┐
│  ⛓️  UNICHAIN BLOCKCHAIN                                     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  Universal Router → Uniswap V4                               │
│  Swap ejecutado                                              │
│  Tx confirmada ✅                                            │
└──────────────────────────────────────────────────────────────┘
                            │
                            ↓
┌──────────────────────────────────────────────────────────────┐
│  📱 RESULTADO EN APP                                         │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  ✅ Swap Completado!                                         │
│  Recibiste: 10.15 USDC                                       │
│  Gas pagado: $0.00 (Sponsored)                               │
└──────────────────────────────────────────────────────────────┘
```

---

## 🏗️ Estructura del Backend (Árbol de Archivos)

```
voiceswap/
│
├── src/
│   │
│   ├── config/
│   │   ├── networks.ts         ← Chains, RPCs, contracts
│   │   └── index.ts
│   │
│   ├── routes/
│   │   ├── swap.ts             ← /quote, /route, /execute, /status
│   │   └── voice.ts            ← /voice-command (TODO)
│   │
│   ├── services/
│   │   ├── uniswap.ts          ← Uniswap V4 (quotes, routes, calldata)
│   │   ├── thirdwebEngine.ts   ← Account Abstraction, gas sponsorship
│   │   ├── thirdwebAI.ts       ← AI Chat integration (TODO)
│   │   └── translation.ts      ← ES→EN translation (TODO)
│   │
│   ├── middleware/
│   │   ├── x402.ts             ← Micropayments ($0.001-$0.02 por request)
│   │   └── validation.ts       ← Zod schemas
│   │
│   ├── types/
│   │   ├── api.ts              ← TypeScript types
│   │   └── contracts.ts
│   │
│   └── index.ts                ← Express server, CORS, rate limiting
│
├── .env                        ← Secret keys, wallet addresses
├── package.json
├── tsconfig.json
└── test_endpoints.sh           ← Script de testing
```

---

## 🔄 Flujo de Datos: Swap Completo

```
┌─────────────┐
│ Mobile App  │
└──────┬──────┘
       │
       │ POST /execute
       │ { tokenIn, tokenOut, amountIn, useEngine: true }
       ↓
┌──────────────────────────┐
│ x402 Middleware          │
│ Payment: $0.02           │
│ ✅ Verified              │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│ swap.ts (Route Handler)  │
│ - Validate request       │
│ - Check useEngine flag   │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│ UniswapService.getRoute()│
│ ┌──────────────────────┐ │
│ │ 1. getQuote()        │ │
│ │    ├─ findBestPool() │ │
│ │    ├─ Quoter V4      │ │
│ │    └─ priceImpact    │ │
│ │                      │ │
│ │ 2. encodeCalldata()  │ │
│ │    ├─ SWAP_EXACT_IN  │ │
│ │    ├─ SETTLE_ALL     │ │
│ │    └─ TAKE_ALL       │ │
│ └──────────────────────┘ │
└──────┬───────────────────┘
       │
       │ calldata: "0x3593564c..."
       ↓
┌──────────────────────────────────────┐
│ ThirdwebEngine.executeSwapViaEngine()│
│ ┌──────────────────────────────────┐ │
│ │ 1. GET smart account for user    │ │
│ │                                  │ │
│ │ 2. POST /transactions            │ │
│ │    {                             │ │
│ │      chainId: 130,               │ │
│ │      from: backend_wallet,       │ │
│ │      to: Universal_Router,       │ │
│ │      data: calldata,             │ │
│ │      sponsorGas: true ✅         │ │
│ │    }                             │ │
│ └──────────────────────────────────┘ │
└──────┬───────────────────────────────┘
       │
       │ queueId: "tx-1234567890"
       ↓
┌──────────────────────────┐
│ Thirdweb Engine API      │
│ ┌──────────────────────┐ │
│ │ ERC-4337 Flow:       │ │
│ │                      │ │
│ │ UserOperation        │ │
│ │    ↓                 │ │
│ │ Paymaster (Gas)      │ │
│ │    ↓                 │ │
│ │ Bundler              │ │
│ │    ↓                 │ │
│ │ Submit to Unichain   │ │
│ └──────────────────────┘ │
└──────┬───────────────────┘
       │
       │ txHash: "0xabc..."
       ↓
┌──────────────────────────┐
│ Unichain Blockchain      │
│ ┌──────────────────────┐ │
│ │ Universal Router     │ │
│ │    ↓                 │ │
│ │ Uniswap V4 Pool      │ │
│ │    ↓                 │ │
│ │ Swap executed ✅     │ │
│ │    ↓                 │ │
│ │ Block confirmed      │ │
│ └──────────────────────┘ │
└──────┬───────────────────┘
       │
       │ status: "confirmed"
       ↓
┌──────────────────────────┐
│ Mobile App               │
│ ┌──────────────────────┐ │
│ │ Poll /status/tx-...  │ │
│ │                      │ │
│ │ pending → confirmed  │ │
│ │                      │ │
│ │ ✅ Swap Complete!    │ │
│ └──────────────────────┘ │
└──────────────────────────┘
```

---

## 🌐 Integraciones Externas

```
                    ┌─────────────────────┐
                    │  VoiceSwap Backend  │
                    │   (Express Server)  │
                    └──────────┬──────────┘
                               │
                ┌──────────────┼──────────────┐
                │              │              │
                ↓              ↓              ↓
     ┌──────────────┐ ┌──────────────┐ ┌─────────────┐
     │  Thirdweb    │ │  Unichain    │ │  OpenAI     │
     │  APIs        │ │  RPC         │ │  (Optional) │
     └──────────────┘ └──────────────┘ └─────────────┘
          │                 │                 │
          │                 │                 │
     ┌────┴────┐       ┌────┴────┐       ┌────┴────┐
     │         │       │         │       │         │
     ↓         ↓       ↓         ↓       ↓         ↓
┌────────┐ ┌────────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐
│  AI    │ │Engine  │ │Quote │ │ Send │ │Trans-│ │ Chat │
│  Chat  │ │  API   │ │ Call │ │  Tx  │ │late  │ │ GPT-4│
└────────┘ └────────┘ └──────┘ └──────┘ └──────┘ └──────┘
           (Gas sponsor)  (V4)  (AA)     (ES→EN)
```

---

## 📊 Endpoints del Backend

```
┌──────────────────────────────────────────────────────────────┐
│  🌐 API ENDPOINTS                                            │
└──────────────────────────────────────────────────────────────┘

GET  /health
     └─ Status del servidor
     └─ Features: AA, x402, V4
     └─ FREE

GET  /tokens
     └─ Lista de tokens soportados
     └─ FREE

GET  /quote
     ├─ Query: tokenIn, tokenOut, amountIn
     ├─ x402: $0.001
     └─ Response: Quote con priceImpact

POST /route
     ├─ Body: tokenIn, tokenOut, amountIn, recipient
     ├─ x402: $0.005
     └─ Response: Quote + calldata + deadline

POST /execute
     ├─ Body: swap params + useEngine
     ├─ x402: $0.02
     ├─ IF useEngine=true:
     │   └─ Thirdweb Engine (gas sponsored)
     └─ IF useEngine=false:
         └─ Direct execution (relayer wallet)

GET  /status/:txHash
     ├─ Path param: transaction hash / queueId
     ├─ x402: $0.001
     └─ Response: pending/confirmed/failed

POST /voice-command (TODO)
     ├─ Body: transcript, userAddress
     ├─ x402: TBD
     └─ Response: swap description + queueId
```

---

## 🔐 Autenticación y Seguridad

```
┌─────────────────────────────────────────────────────────────┐
│  MOBILE APP AUTHENTICATION                                   │
└─────────────────────────────────────────────────────────────┘

Usuario abre app
    ↓
Elige método:
    ├─ Email (OTP)
    ├─ Apple Sign-In
    ├─ Google Sign-In
    └─ Guest Mode
    ↓
Thirdweb In-App Wallet
    ↓
POST /v1/auth/initiate
    ↓
POST /v1/auth/complete
    ↓
Wallet creada automáticamente
    ├─ EOA (Externally Owned Account)
    └─ Smart Account (ERC-4337)
    ↓
Session Key creada
    ├─ approvedTargets: [Universal Router]
    ├─ nativeTokenLimit: 0.1 ETH
    └─ validUntil: 1 hour
    ↓
Guardada en SecureStore (encrypted)
    ↓
Usuario puede hacer múltiples swaps sin re-firmar ✅


┌─────────────────────────────────────────────────────────────┐
│  BACKEND SECURITY                                            │
└─────────────────────────────────────────────────────────────┘

Request llega
    ↓
[Helmet] - Security headers
    ↓
[CORS] - Only allowed origins
    ↓
[Rate Limiter] - 100 req/15min
    ↓
[x402 Middleware] - Payment verification
    ↓
[Zod Validation] - Schema validation
    ↓
[Route Handler] - Business logic
    ↓
Response
```

---

## 💰 Modelo de Monetización (x402)

```
┌─────────────────────────────────────────────────────────────┐
│  x402 MICROPAYMENTS                                          │
└─────────────────────────────────────────────────────────────┘

Precios por endpoint:
    /quote      → $0.001
    /route      → $0.005
    /execute    → $0.02
    /status     → $0.001

Ejemplo de uso:
    1. GET /quote         → $0.001
    2. POST /execute      → $0.02
    3. GET /status (x5)   → $0.005
    ──────────────────────────────
    Total por swap:        $0.026

Volumen esperado:
    100 swaps/día × $0.026 = $2.60/día
    3,000 swaps/mes = $78/mes revenue

Pago procesado en:
    Arbitrum (mainnet o sepolia)
    Via Thirdweb x402 facilitator

Payment flow:
    User Request
        ↓
    X-Payment header
        ↓
    Thirdweb settlePayment()
        ↓
    If valid → Allow request
    If invalid → 402 Payment Required
```

---

## 🚀 Deploy Pipeline (Futuro)

```
┌──────────────┐
│ Local Dev    │
│ localhost    │
└──────┬───────┘
       │
       │ git push
       ↓
┌──────────────┐
│ GitHub       │
│ main branch  │
└──────┬───────┘
       │
       │ Auto deploy
       ↓
┌──────────────┐
│ Railway/     │
│ Render       │
└──────┬───────┘
       │
       │ Production URL
       ↓
┌──────────────────────┐
│ https://api.         │
│ voiceswap.com        │
└──────────────────────┘
       │
       │ Used by
       ↓
┌──────────────────────┐
│ Mobile App (iOS)     │
│ App Store            │
└──────────────────────┘
```

---

## 📈 Métricas de Performance

```
Backend Response Times:
    /health     →   50-100ms   (simple health check)
    /quote      →  500-800ms   (RPC calls to quoter)
    /route      →  800-1200ms  (quote + encoding)
    /execute    → 1500-2500ms  (Thirdweb Engine queue)
    /status     →  200-400ms   (RPC call)

Voice Command Flow (end-to-end):
    Audio capture        →  100-200ms
    Speech-to-text       →  500-1000ms
    Translation          →  500-800ms
    Thirdweb AI          → 1000-1500ms
    Execute              → 1500-2500ms
    ─────────────────────────────────
    Total:                 3600-6500ms (~4-6 segundos)

Blockchain Confirmation:
    Submit → Pending     →  2-5 segundos
    Pending → Confirmed  → 10-30 segundos
    Total time:            12-35 segundos
```

---

**Estos diagramas muestran:**
- ✅ Flujo completo del usuario
- ✅ Estructura del backend
- ✅ Integraciones externas
- ✅ Seguridad y autenticación
- ✅ Monetización x402
- ✅ Performance metrics

**Archivos creados:**
- [ARCHITECTURE.md](ARCHITECTURE.md) - Arquitectura detallada
- [DIAGRAMS.md](DIAGRAMS.md) - Diagramas visuales
